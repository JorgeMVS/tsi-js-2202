console.log(window);

 let meuTitulo = document.getElementById('titulo');
 let navegador = window.navigator.vendor;
 
 alert('você está usando o navegador: ' +navegador);

 
/*
    if(confirm('Deixe sua mensagem')){
        let msg= prompt('Mensagem');
        meuTitulo.innerText = msg;
        meuTitulo.style.background='green';

    }

 
    else
    {
        meuTitulo.innerText='sem mensagem';
        meuTitulo.style.background='red';
    }
    */